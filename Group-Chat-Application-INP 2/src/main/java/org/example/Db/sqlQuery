create database groupchat;
use groupchat;
CREATE TABLE NewUser(
                     username VARCHAR(20) PRIMARY KEY,
                     password VARCHAR(20),
                     image LONGBLOB
);

create table chat(
userName varchar(20),
id varchar(20),
massege varchar(500),
image longblob,
time varchar(30)
);